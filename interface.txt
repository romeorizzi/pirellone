function is_solvable(m, n) callbacks {
   function is_on(row, col);
}

procedure solve(n, m) callbacks {
    function is_on(row, col);
    procedure switch_row(row);
    procedure switch_col(col);
}

main {
    read choice;
    if choice {
        read n, m;
        call res = is_solvable(m, n);
        write res;
    } else {
        read n, m;
        call solve(n, m);
    }
}
